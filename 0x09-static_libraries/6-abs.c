#include "main.h"

/**
 * _abs - This computes the absolute value of an int
 * @n: The integer to check
 * Return: The absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

